// Write a js program to print all natural numbers in reverse (from n to 1).

function printReverse(num){
    let i = 1;
    while (i<=num) {
        console.log(num);
        num--;
        
    }
}

printReverse(10);