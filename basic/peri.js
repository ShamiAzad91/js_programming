// Write a js program to enter length and breadth of a rectangle and find its perimeter.

function getPerimeter(length,breadth){
    return 2*(length + breadth);
}

const result = getPerimeter(10,34);
console.log(`perimeter of rectangle  is  ${result}`);
